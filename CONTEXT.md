# Domain Glossary

## Task Operations
A deep domain module responsible for orchestrating the lifecycle and state transitions of Tasks (creation, LexoRank-based reordering, cascade deletion) independent of the network or protocol (HTTP, Socket.io) used to invoke them.
