{
    "name": "collab-app",
    "private": true,
    "scripts": {
        "dev": "turbo dev",
        "build": "turbo build",
        "test": "turbo test",
        "lint": "turbo lint",
        "clean": "turbo clean && rm -rf node_modules"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "turbo": "^2.0.0",
        "typescript": "^5.4.0"
    },
    "packageManager": "pnpm@10.33.0",
    "engines": {
        "node": ">=20.0.0",
        "pnpm": ">=9.0.0"
    }
}